import styles from "../styles/Footer.module.css"

const Footer: React.FC = () => {
  return (
    <footer className={`${styles.headerBase} mt-8 sm:flex-row flex-col flex items-center justify-evenly sm:justify-between px-8 sm:px-12`}>
    </footer>
  )
}
export default Footer